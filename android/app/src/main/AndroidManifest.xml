<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto">

    <!-- libultraship renders with OpenGL ES 3 -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.type.pc" android:required="false" />

    <!-- Rumble Pak emulation through SDL haptics -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- No storage permissions: the ROM is imported through the system file picker and
         lives in this app's private external files directory. -->

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:allowBackup="false"
        android:hasFragileUserData="true"
        android:appCategory="game"
        android:isGame="true"
        android:hardwareAccelerated="true"
        android:allowNativeHeapPointerTagging="false"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

        <!-- Let the activity see the back button so it can open the settings menu -->
        <meta-data android:name="SDL_ENV.SDL_ANDROID_TRAP_BACK_BUTTON" android:value="1" />
        <!-- Touches on the game surface reach the in-game settings menu as mouse events -->
        <meta-data android:name="SDL_ENV.SDL_TOUCH_MOUSE_EVENTS" android:value="1" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:preferMinimalPostProcessing="true"
            android:screenOrientation="sensorLandscape"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
        </activity>
    </application>
</manifest>
